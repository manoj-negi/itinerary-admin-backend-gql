scalar Time
scalar UUID

type Query

type Mutation {
  login(email: String!, password: String!): LoginResponse!
   logout: Boolean!
  }

type LoginResponse {
  token: String!
  user: User!
}
type UploadURL {
  uploadUrl: String!
  publicUrl: String!
}

extend type Mutation {
  getUploadUrl(
    folder: String!,
    fileName: String!,
    contentType: String!
  ): UploadURL!
}